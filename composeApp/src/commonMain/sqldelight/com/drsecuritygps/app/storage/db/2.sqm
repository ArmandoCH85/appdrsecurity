-- Dispositivos que quedaron en v2 sin esta tabla (instalación/migración previa) reciben el esquema al pasar a v3.
CREATE TABLE IF NOT EXISTS alert_event_notified (
    event_id TEXT NOT NULL PRIMARY KEY,
    payload TEXT NOT NULL,
    notified_at INTEGER NOT NULL
);
